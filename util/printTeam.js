function printTeam(x) {
    x.map(function(y) {
        let name = y.name;
        let id = y.id;
        let email  = y.email;
        let role;
        if (i) {
            role = y.officeNumber;
        } else if (i) {
            role = y.github;
        } else if (i) {
            role = y.school;
        }
    })
}

